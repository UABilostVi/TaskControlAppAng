import { DashboardDirective } from './dashboard/ref.directive';

describe('DashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardDirective();
    expect(directive).toBeTruthy();
  });
});
